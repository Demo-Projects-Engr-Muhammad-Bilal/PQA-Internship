{
  "name": "pqa_internship_project",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "prisma generate --schema=./packages/db/prisma/schema.prisma",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@9.0.0"
}